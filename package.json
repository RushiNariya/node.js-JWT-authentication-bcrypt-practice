{
  "name": "nodejs",
  "version": "1.0.0",
  "description": "jwt token project",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "server": "nodemon -r dotenv/config app.js dotenv_config_path=./variables.env",
    "start": "node -r dotenv/config app.js dotenv_config_path=./variables.env"
  },
  "author": "rushi nariya",
  "license": "ISC",
  "dependencies": {
    "bcrypt": "^5.0.1",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "jsonwebtoken": "^8.5.1",
    "pg": "^8.5.1"
  },
  "devDependencies": {
    "nodemon": "^2.0.7"
  }
}
